/**
 * NEAR Authentication Types
 * Defines types for NEAR authentication data
 */
import { Types } from '../../../deps.ts';

// Re-export the NearAuthData type from the types package
export type NearAuthData = Types.NearAuthData;
